# Claude is served via AWS Bedrock. AWS credentials are picked up from the
# standard AWS credential chain (~/.aws/credentials, AWS_ACCESS_KEY_ID /
# AWS_SECRET_ACCESS_KEY env vars, or an assumed role) - no key goes here.
# Only the region needs to be set if it's not us-east-1.
AWS_REGION=us-east-1
